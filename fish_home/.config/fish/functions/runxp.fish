# Defined in /tmp/afsmpca/fish.fCaHqA/runxp.fish @ line 2
function runxp
	echo (tput setab 3)"Put TWO \"\$\" after \"the\" password (even though it's two for the email account"(tput sgr 0)
   nohup rdesktop -g 1900x1000 eccmcwts5 -d ECQUEBEC -u CarphinP > /dev/null &
end
